/*
 * Day 11 — Exercises
 * Compile: gcc exercises.c -o exercises.exe
 */
#include <stdio.h>
#include <stdlib.h>
int main(void) {
    /* Exercise 1: Dynamically allocate an array of n doubles (user input),
       fill with 1.0/1, 1.0/2, ... 1.0/n, print sum.
       TODO: */

    /* Exercise 2: Dynamically allocate a 2D matrix (rows x cols via malloc),
       fill with row+col, print it, then free.
       TODO: */

    /* Exercise 3: Write a function dup_string(const char *s) that returns a
       heap-allocated copy of s. Remember to free it.
       TODO: */

    /* Exercise 4: Write a growable int array (starts at capacity 2, doubles
       when full). Push 10 values. Print capacity growth.
       TODO: */

    /* Exercise 5: Read n integers from user into a dynamic array, sort them
       with qsort, print sorted result, free.
       TODO: */

    return 0;
}
