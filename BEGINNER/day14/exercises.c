/*
 * Day 14 — Exercises
 * These build on the capstone to add more features.
 * Compile: gcc exercises.c -o exercises.exe
 */
#include <stdio.h>
int main(void) {
    /* Exercise 1: Add a "edit" feature to the capstone: find by ID, modify GPA.
       TODO: */

    /* Exercise 2: Add statistics: print average GPA, highest GPA student.
       TODO: */

    /* Exercise 3: Add input validation: reject GPA < 0 or > 4, reject age < 5.
       TODO: */

    /* Exercise 4: Save the DB in binary format instead of CSV.
       (Hint: use fwrite(&s, sizeof(Student), 1, f) for each record)
       TODO: */

    /* Exercise 5: Sort alphabetically by name using strcmp in qsort comparator.
       TODO: */

    return 0;
}
