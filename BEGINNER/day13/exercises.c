/*
 * Day 13 — Exercises
 * Compile: gcc exercises.c -o exercises.exe
 */
#include <stdio.h>
int main(void) {
    /* Exercise 1: Define a macro CLAMP(x, lo, hi) that clamps x in [lo,hi].
       Test it with several values.
       TODO: */

    /* Exercise 2: Create a header "greet.h" and "greet.c" with a function
       greet(const char *name) that prints "Hello, <name>!".
       Include and call it here (or in a separate main file).
       TODO: */

    /* Exercise 3: Use #ifdef to print an "Easter egg" only when SPECIAL is
       defined (pass -DSPECIAL to gcc on command line).
       TODO: */

    /* Exercise 4: Print __FILE__, __LINE__, __DATE__, __TIME__ from macros.
       TODO: */

    /* Exercise 5: Write a macro SWAP(type, a, b) using a temp variable.
       Test it on ints and floats.
       TODO: */

    return 0;
}
