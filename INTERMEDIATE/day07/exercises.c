/* Int Day 07 Exercises */
#include <stdio.h>
int main(void) {
    /* Ex 1: Count the number of 1-bits without a loop (use Kernighan's method: n &= n-1). */
    /* Ex 2: Swap two integers using XOR (no temp variable). */
    /* Ex 3: Reverse all bits in a uint32_t. */
    /* Ex 4: Find the only non-repeated element in an array where all others appear twice (XOR trick). */
    /* Ex 5: Implement a simple bitset: set, clear, test, count operations on a uint64_t. */
    return 0;
}
