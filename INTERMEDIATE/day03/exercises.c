/* Int Day 03 Exercises */
#include <stdio.h>
int main(void) {
    /* Ex 1: Write a function that checks if a BST is valid (all left < root < all right). */
    /* Ex 2: Find the lowest common ancestor (LCA) of two nodes in a BST. */
    /* Ex 3: Level-order (BFS) traversal of a binary tree using a queue. */
    /* Ex 4: Count leaf nodes and internal nodes. */
    /* Ex 5: Implement a function to convert a sorted array to a balanced BST. */
    return 0;
}
