/*
 * Intermediate Day 01 — Exercises
 * Compile: gcc exercises.c -o exercises.exe
 */
#include <stdio.h>
int main(void) {
    /* Ex 1: Implement a function that detects a cycle in a linked list (Floyd's algorithm). */
    /* Ex 2: Write a function that finds the middle node of a singly linked list. */
    /* Ex 3: Implement a function to remove all duplicates from a sorted linked list. */
    /* Ex 4: Write a function that rotates a linked list by k positions. */
    /* Ex 5: Implement a doubly-linked list with push_front, pop_front, pop_back. */
    return 0;
}
