/* Int Day 10 Exercises */
#include <stdio.h>
int main(void) {
    /* Ex 1: Add a "sort by priority" command to the capstone. */
    /* Ex 2: Add due-dates (as strings) to tasks. Sort by due date. */
    /* Ex 3: Add a "completed today" counter using a bitmask. */
    /* Ex 4: Export tasks as JSON instead of CSV. */
    /* Ex 5: Implement undo of the last delete operation. */
    return 0;
}
