/* Int Day 02 Exercises — Compile: gcc exercises.c -o exercises.exe */
#include <stdio.h>
int main(void) {
    /* Ex 1: Implement a circular queue (array-based) with fixed capacity. */
    /* Ex 2: Use a stack to reverse a string in-place. */
    /* Ex 3: Implement a min-stack: a stack that also returns the current minimum in O(1). */
    /* Ex 4: Use two stacks to simulate a queue. */
    /* Ex 5: BFS of a simple graph using a queue (represent as adjacency list). */
    return 0;
}
