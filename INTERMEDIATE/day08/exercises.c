/* Int Day 08 Exercises */
#include <stdio.h>
int main(void) {
    /* Ex 1: Implement a pool allocator with variable block sizes. */
    /* Ex 2: Write a garbage-collector-style reference counting wrapper. */
    /* Ex 3: Use mmap-style simulation: reserve a large buffer, hand out slices. */
    /* Ex 4: Implement a simple slab allocator for one struct type. */
    /* Ex 5: Measure how many recursive calls until stack overflow (carefully!). */
    return 0;
}
