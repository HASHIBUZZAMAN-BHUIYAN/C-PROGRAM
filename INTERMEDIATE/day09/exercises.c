/* Int Day 09 Exercises */
#include <stdio.h>
int main(void) {
    /* Ex 1: Create 5 threads, each printing its ID and a message. Ensure all finish before main exits. */
    /* Ex 2: Implement a thread-safe counter using an atomic (InterlockedIncrement on Windows). */
    /* Ex 3: Use a mutex to protect a shared linked list from concurrent push/pop. */
    /* Ex 4: Implement a thread pool with a fixed number of worker threads and a task queue. */
    /* Ex 5: Write a parallel mergesort that sorts each half on a separate thread. */
    return 0;
}
